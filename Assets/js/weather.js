$(document).ready(function(){

    //get current date from Moment.js
    var momentDateString = moment().toString();
    var momentDate = moment().toDate();

    console.log(momentDateString);
    console.log(momentDate);

    /*
    //Create dynamic HTML elements
    var row1DivEl = $("<div>");
    row1DivEl.addClass("row");
    var

    var row2DivEl = $("<div>");
    row2DivEl.addClass("row");
    $(".container").append(rowDivEl, row2DivEl);
    
    var
*/

})